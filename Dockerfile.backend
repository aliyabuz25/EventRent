FROM node:22-alpine
WORKDIR /app
RUN apk add --no-cache python3 make g++ sqlite-dev
COPY package*.json ./
RUN npm ci --omit=dev && apk del python3 make g++ sqlite-dev
COPY server.js ./
COPY data ./data
RUN mkdir -p /app/uploads
EXPOSE 4320
ENV HOST=0.0.0.0
CMD ["node", "server.js"]
